import { checkCorrectWord, checkArray } from '../shared/utils';
export declare function checkMultiCorrectResult(numbers: number[], result: number | null, firstOperand: string, secondOperand: string, thirdOperand: string): boolean;
export { checkArray, checkCorrectWord };
